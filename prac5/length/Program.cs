using System;

class Program
{
    static void Main(string[] args)
    {
        // Declare a string
        string sampleString = "my name is anshuman";
        
        // Display the string
        Console.WriteLine("Original String: " + sampleString);
        
        // Use the Length property to find the length of the string
        int stringLength = sampleString.Length;
        
        // Display the length of the string
        Console.WriteLine("Length of the string: " + stringLength);
        
    
        
        // Wait for user input before closing the console
        Console.ReadLine();
    }
}
